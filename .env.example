# Plantilla de variables de entorno.
# Copiar a `.env` y completar con los valores reales.
# IMPORTANTE: el archivo `.env` está en `.gitignore`, no debe commitearse.

DATABASE_URL=postgresql://usuario:password@localhost:5432/csmc_paquetes
